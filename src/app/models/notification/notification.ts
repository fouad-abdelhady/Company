import { staffMember } from "../staff/userTeamRes";

export type NotificationItem = {
    id: number;
    title: string;
    arTitle: string;
    description: string;
    arDescription: string;
    poster: staffMember;
    createdAt: string;
};
  
  
  
  
  
  